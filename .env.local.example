# Server-checked password for the /admin page. Change this.
WORKHUB_ADMIN_PASSWORD=change-me-now

# Production storage (Vercel KV / Upstash Redis). Leave empty on localhost
# to use the local JSON file fallback (data/tools.json).
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
